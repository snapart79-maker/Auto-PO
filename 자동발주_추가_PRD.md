# 자동발주 시스템 추가 PRD

## UI 개선 및 재고관리 기능 추가

### Product Requirements Document (PRD) v1.4 - 추가 사항

---

| 항목 | 내용 |
|------|------|
| **문서 버전** | v1.4 (추가 PRD) |
| **작성일** | 2025년 12월 28일 |
| **작성자** | 생산관리실 |
| **기준 문서** | 자동발주시스템_PRD_v1.3.md |
| **목적** | UI 구조 변경, 재고관리 기능 추가, 발주 프로세스 개선 |

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v1.3 | 2025-12-27 | 기존 PRD (회사정보, 업로드검증, 이원화공급, 베트남특수성, 감사로그) |
| v1.4 | 2025-12-28 | UI 구조 전면 개편, 재고관리 추가, MRP 로직 상세화, 디자인 가이드라인 |

---

## 목차

1. [개요](#1-개요)
2. [메뉴 구조 변경](#2-메뉴-구조-변경)
3. [UI/UX 디자인 가이드라인](#3-uiux-디자인-가이드라인)
4. [입고/출고 현황 화면](#4-입고출고-현황-화면)
5. [재고 관리 기능](#5-재고-관리-기능)
6. [발주 관리 기능](#6-발주-관리-기능)
7. [MRP 계산 로직 상세](#7-mrp-계산-로직-상세)
8. [데이터베이스 추가 설계](#8-데이터베이스-추가-설계)
9. [개발 가이드라인](#9-개발-가이드라인)
10. [개발 일정](#10-개발-일정)

---

## 1. 개요

### 1.1 추가 PRD 목적

본 문서는 기존 PRD v1.3을 보완하여 다음 사항을 추가로 정의합니다:

- **UI 구조 전면 개편**: 메뉴 그룹핑 및 화면 레이아웃 변경
- **재고 관리 기능 추가**: 현재고 조회, 초기재고, 재고조정
- **발주 프로세스 명확화**: 출고계획 → MRP 계산 → 발주 흐름
- **디자인 가이드라인**: 심플하고 세련된 버튼식 UI

### 1.2 핵심 변경 요약

| 구분 | 기존 (v1.3) | 변경 (v1.4) |
|------|------------|------------|
| 메뉴 구조 | 단일 레벨 사이드바 | **메인헤더 그룹 + 서브메뉴** |
| 입고/출고 | 업로드 중심 | **현황 조회 중심 + 업로드** |
| 재고 관리 | 없음 | **신규 추가** |
| 발주 관리 | MRP 관리 + 발주관리 분리 | **통합 발주 관리** |
| 평균 납품량 기준 | 30일 | **1년 (365일), 사용자 설정 가능** |

---

## 2. 메뉴 구조 변경

### 2.1 기존 메뉴 구조

```
Auto PO
├── 대시보드
├── 회사정보
├── 차종관리
├── 거래처관리
├── 제품관리
├── 환율관리
├── 데이터 업로드
│   ├── 입출고 업로드
│   └── 출고계획 업로드
├── MRP 관리
│   ├── MRP 계산
│   └── 베트남 발주
└── 발주 관리
    └── 발주관리
```

### 2.2 변경된 메뉴 구조

```
Auto PO
│
├── 📊 대시보드
│
├── ⚙️ 1. 기준관리 ─────────────────────────────────
│   ├── 회사정보
│   ├── 차종관리
│   ├── 거래처관리
│   ├── 제품관리
│   └── 환율관리
│
├── 📦 2. 입고/출고 ─────────────────────────────────
│   ├── 입고 현황
│   └── 출고 현황
│
├── 📋 3. 재고 관리 ───────────────────────────────── (신규)
│   ├── 재고 현황
│   ├── 초기 재고 등록
│   └── 재고 조정
│
├── 🛒 4. 발주 관리 ─────────────────────────────────
│   ├── 출고 계획 등록
│   ├── MRP 계산 / 발주 권고
│   ├── 발주서 관리
│   └── 베트남 주간 발주
│
└── 📈 5. 보고서 ───────────────────────────────────── (Phase 2)
    └── (추후 정의)
```

### 2.3 메뉴 그룹 설명

| 메인헤더 | 역할 | 서브메뉴 |
|---------|------|---------|
| **기준관리** | 시스템 기초 데이터 설정 | 회사정보, 차종, 거래처, 제품, 환율 |
| **입고/출고** | 입출고 실적 조회 및 등록 | 입고 현황, 출고 현황 |
| **재고 관리** | 재고 현황 파악 및 조정 | 재고 현황, 초기재고, 재고조정 |
| **발주 관리** | 발주 계획 및 실행 | 출고계획, MRP, 발주서, 베트남 |
| **보고서** | 분석 및 리포트 | (Phase 2) |

---

## 3. UI/UX 디자인 가이드라인

### 3.1 디자인 원칙

| 원칙 | 설명 |
|------|------|
| **심플함** | 불필요한 요소 제거, 핵심 기능에 집중 |
| **버튼식 메뉴** | 클릭 가능한 버튼 형태의 메뉴 구성 |
| **세련된 디자인** | 모던하고 깔끔한 UI, 적절한 여백 |
| **일관성** | 모든 화면에서 동일한 레이아웃 패턴 |

### 3.2 레이아웃 패턴

모든 현황/조회 화면은 아래 패턴을 따릅니다:

```
┌─────────────────────────────────────────────────────────────────────────┐
│  📦 화면 제목                                        [조회] [새로고침] │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─ 조회조건 ─────────────────────────────┐  ┌─ 기능 버튼 ───────────┐ │
│  │ 기간: [시작일] ~ [종료일]              │  │  [개별등록]           │ │
│  │ 품번: [________________]               │  │  [엑셀 업로드]        │ │
│  │ 품명: [________________]               │  │  [엑셀 다운로드]      │ │
│  │ 거래처: [________________]             │  │                       │ │
│  │                          [조회] [초기화]│  └───────────────────────┘ │
│  └────────────────────────────────────────┘                             │
│                                                                         │
├────────────────────────────────────────────┬────────────────────────────┤
│  [메인 테이블]                              │  [사이드 요약]             │
│  ┌────────────────────────────────────────┐│  ┌────────────────────────┐│
│  │  컬럼1 | 컬럼2 | 컬럼3 | 컬럼4 | ...   ││  │  거래처별 합계 (원화)  ││
│  │────────────────────────────────────────││  │  ───────────────────   ││
│  │  데이터 행                             ││  │  거래처A | 500 | 1,000 ││
│  │  데이터 행                             ││  │  거래처B | 300 | 600   ││
│  │  ...                                   ││  │  합계    | 800 | 1,600 ││
│  │                                        ││  ├────────────────────────┤│
│  │                                        ││  │  거래처별 합계 (외화)  ││
│  │                                        ││  │  ───────────────────   ││
│  │                                        ││  │  거래처C | 200 | $150  ││
│  └────────────────────────────────────────┘│  └────────────────────────┘│
│  합계: 1,000건                             │                            │
│  ◀ 1 2 3 4 5 ▶                            │                            │
└────────────────────────────────────────────┴────────────────────────────┘
```

### 3.3 컬러 스킴

| 용도 | 색상 | HEX |
|------|------|-----|
| Primary (메인) | 파란색 | #3B82F6 |
| Success (성공/정상) | 초록색 | #22C55E |
| Warning (주의) | 노란색 | #F59E0B |
| Danger (위험/긴급) | 빨간색 | #EF4444 |
| Background | 연한 회색 | #F8FAFC |
| Border | 회색 | #E2E8F0 |

### 3.4 버튼 스타일

| 타입 | 용도 | 스타일 |
|------|------|--------|
| **Primary** | 주요 액션 (조회, 저장) | 파란 배경 + 흰 텍스트 |
| **Secondary** | 보조 액션 (초기화, 취소) | 흰 배경 + 파란 테두리 |
| **Danger** | 삭제, 위험 액션 | 빨간 배경 + 흰 텍스트 |
| **Icon Button** | 아이콘만 있는 버튼 | 투명 배경 + 아이콘 |

### 3.5 MCP 활용 (Figma)

UI 개발 시 **figma MCP**를 활용하여 와이어프레임 및 디자인 시안을 생성합니다.

```
개발 순서:
1. figma MCP → 화면별 와이어프레임 생성
2. 디자인 검토 및 피드백
3. shadcn + magic MCP → 실제 컴포넌트 구현
```

---

## 4. 입고/출고 현황 화면

### 4.1 입고 현황

#### 4.1.1 조회조건

| 필드 | 입력 방식 | 설명 |
|------|----------|------|
| 기간 | Date Picker | 시작일 ~ 종료일 |
| 품번 | **텍스트 입력** | 콤마(,)로 여러 개 입력 가능 |
| 품명 | **텍스트 입력** | 부분 검색 (LIKE '%검색어%') |
| 거래처 | **텍스트 입력** | 콤마(,)로 여러 개 입력 가능 |

> 📌 **드롭다운이 아닌 텍스트 입력** 방식으로, 여러 개를 동시에 조회할 수 있습니다.

#### 4.1.2 메인 테이블 컬럼

| 컬럼 | 타입 | 설명 |
|------|------|------|
| 입고일 | Date | 입고 날짜 |
| 거래처 | String | 공급사명 |
| 품번 | String | 고객품번 |
| 품명 | String | 제품명 |
| 수량 | Integer | 입고 수량 |
| 단가 | Decimal | 적용단가 |
| 공급가액 | Decimal | 수량 × 단가 |
| 화폐단위 | String | KRW, USD 등 |
| 총금액 | Decimal | 최종 금액 |

#### 4.1.3 사이드 요약 (거래처별 합계)

**원화 합계:**
| 컬럼 | 설명 |
|------|------|
| 거래처 | 공급사명 |
| 입고수량 합계 | 해당 거래처 총 수량 |
| 공급가액 합계 | 해당 거래처 총 금액 |

**외화 합계:**
| 컬럼 | 설명 |
|------|------|
| 거래처 | 공급사명 |
| 입고수량 합계 | 해당 거래처 총 수량 |
| 공급가액 합계 | 해당 거래처 총 금액 |
| 화폐단위 | USD, EUR 등 |

#### 4.1.4 기능 버튼

| 버튼 | 기능 |
|------|------|
| **개별 등록** | 입고 데이터 1건씩 직접 입력 |
| **엑셀 업로드** | MES Excel 파일 업로드 (고급 검증 적용) |
| **엑셀 다운로드** | 조회 결과 Excel 내보내기 |

### 4.2 출고 현황

입고 현황과 **동일한 구조**로 구성합니다.

| 항목 | 입고 현황 | 출고 현황 |
|------|----------|----------|
| 날짜 필드 | 입고일 | **출하일** |
| 거래처 | 공급사 (협력사) | **고객사** |
| 나머지 | 동일 | 동일 |

---

## 5. 재고 관리 기능

### 5.1 재고 계산 로직

```
현재고 = 초기재고 + Σ(입고 수량) - Σ(출고 수량) ± Σ(조정 수량)
```

| 항목 | 데이터 소스 |
|------|------------|
| 초기재고 | initial_inventory 테이블 |
| 입고 수량 | inventory_transactions (type = 'IN') |
| 출고 수량 | inventory_transactions (type = 'OUT') |
| 조정 수량 | inventory_adjustments 테이블 |

### 5.2 재고 현황 화면

#### 5.2.1 조회조건

| 필드 | 입력 방식 | 설명 |
|------|----------|------|
| 품번 | 텍스트 입력 | 콤마(,)로 여러 개 입력 |
| 품명 | 텍스트 입력 | 부분 검색 |
| 차종 | 텍스트 입력 | 콤마(,)로 여러 개 입력 |
| 재고 상태 | 드롭다운 | 전체 / 정상 / 부족 / 위험 |

#### 5.2.2 메인 테이블 컬럼

| 컬럼 | 타입 | 설명 |
|------|------|------|
| 품번 | String | 고객품번 |
| 품명 | String | 제품명 |
| 차종 | String | 차종명 |
| 현재고 | Integer | 계산된 현재 재고 |
| 안전재고 | Integer | 설정된 안전재고 |
| 재고 상태 | Badge | 정상/부족/위험 |
| 입고 예정 | Integer | 발주 후 미입고 수량 |
| 가용 재고 | Integer | 현재고 + 입고예정 |

#### 5.2.3 재고 상태 기준

| 상태 | 조건 | 색상 |
|------|------|------|
| 🟢 정상 | 현재고 ≥ 안전재고 × 70% | 초록 |
| 🟡 주의 | 안전재고 × 50% ≤ 현재고 < 안전재고 × 70% | 노랑 |
| 🟠 경고 | 안전재고 × 30% ≤ 현재고 < 안전재고 × 50% | 주황 |
| 🔴 위험 | 현재고 < 안전재고 × 30% | 빨강 |

### 5.3 초기 재고 등록

시스템 최초 사용 시 또는 기준일 변경 시 초기 재고를 등록합니다.

| 기능 | 설명 |
|------|------|
| 기준일 설정 | 재고 기준 날짜 지정 |
| 개별 등록 | 품목별 초기 재고 입력 |
| 일괄 등록 | Excel 업로드로 대량 등록 |

#### 초기 재고 등록 컬럼

| 컬럼 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 기준일 | Date | Y | 재고 기준 날짜 |
| 품번 | String | Y | 고객품번 |
| 수량 | Integer | Y | 초기 재고 수량 |
| 비고 | String | N | 메모 |

### 5.4 재고 조정

실사 후 차이 발생 시 재고를 조정합니다.

| 기능 | 설명 |
|------|------|
| 조정 유형 | 증가 (+) / 감소 (-) |
| 조정 사유 | 실사차이, 분실, 파손, 기타 |
| 이력 관리 | 모든 조정 내역 기록 |

#### 재고 조정 컬럼

| 컬럼 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 조정일 | Date | Y | 조정 날짜 |
| 품번 | String | Y | 고객품번 |
| 조정 유형 | Enum | Y | INCREASE / DECREASE |
| 조정 수량 | Integer | Y | 조정할 수량 (양수) |
| 조정 사유 | Enum | Y | 실사차이, 분실, 파손, 기타 |
| 비고 | String | N | 상세 메모 |

---

## 6. 발주 관리 기능

### 6.1 발주 프로세스 흐름

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  출고 계획   │────▶│  MRP 계산    │────▶│  발주 권고   │────▶│  발주서 생성 │
│  등록        │     │              │     │              │     │              │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
  고객 납품 계획       소요량 - 재고        권고 목록 표시       협력사/베트남
  일자별/주간별        = 발주 필요량        확인 후 발주         발주서 출력
```

### 6.2 출고 계획 등록

고객사에 납품해야 할 계획을 등록합니다.

#### 6.2.1 계획 유형

| 유형 | 설명 |
|------|------|
| **일자별 (DAILY)** | 특정 날짜에 납품할 수량 |
| **주간별 (WEEKLY)** | 해당 주에 납품할 수량 |

#### 6.2.2 등록 화면 컬럼

| 컬럼 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 계획 유형 | Enum | Y | DAILY / WEEKLY |
| 납품일 (또는 주차) | Date | Y | 납품 예정일 |
| 고객사 | Select | Y | 납품할 고객사 |
| 품번 | Select | Y | 납품할 품목 |
| 수량 | Integer | Y | 납품 수량 |

#### 6.2.3 기능

| 기능 | 설명 |
|------|------|
| 개별 등록 | 계획 1건씩 직접 입력 |
| 엑셀 업로드 | 출고계획 템플릿으로 대량 등록 |
| 조회 | 기간별, 고객사별, 품목별 조회 |

### 6.3 MRP 계산 / 발주 권고

#### 6.3.1 화면 구성

```
┌─────────────────────────────────────────────────────────────────────────┐
│  🛒 MRP 계산 / 발주 권고                         [MRP 재계산] [새로고침] │
├─────────────────────────────────────────────────────────────────────────┤
│  [조회조건]                                                             │
│  기준일: [2025-12-28]   품번: [______]   거래처: [______]              │
│                                                    [조회] [초기화]      │
├─────────────────────────────────────────────────────────────────────────┤
│  [MRP 계산 결과 테이블] ← 화면 진입 시 오늘 기준 자동 계산              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ □ | 품번 | 품명 | 현재고 | 안전재고 | 소요량 | 발주량 | 거래처  │   │
│  │──────────────────────────────────────────────────────────────────│   │
│  │ ☑ | 001  | AAA  | 100   | 50      | 200   | 150    | A사     │   │
│  │ ☑ | 002  | BBB  | 30    | 100     | 500   | 570    | B사     │   │
│  │ □ | 003  | CCC  | 500   | 100     | 200   | 0      | -       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  선택된 항목: 2건                    [선택 발주] [전체 발주] [엑셀 출력] │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 6.3.2 테이블 컬럼

| 컬럼 | 설명 |
|------|------|
| 선택 | 체크박스 (발주 대상 선택) |
| 품번 | 고객품번 |
| 품명 | 제품명 |
| 차종 | 차종명 |
| 현재고 | 현재 재고 수량 |
| 안전재고 | 설정된 안전재고 |
| 소요량 | 출고계획 기반 또는 평균 납품량 |
| 입고예정 | 발주 후 미입고 수량 |
| 발주권고량 | 계산된 발주 필요 수량 |
| 발주처 | 국내 / 베트남 / BOTH |
| 발주 | [발주] 버튼 |

#### 6.3.3 동작 방식

- **화면 진입 시**: 오늘 기준으로 자동 MRP 계산하여 테이블 표시
- **[MRP 재계산]**: 조회조건 변경 후 재계산
- **[선택 발주]**: 체크한 품목만 발주서 생성
- **[전체 발주]**: 발주권고량 > 0 인 전체 품목 발주

### 6.4 발주서 관리

생성된 발주서를 조회/관리합니다.

| 기능 | 설명 |
|------|------|
| 조회 | 기간별, 거래처별, 상태별 조회 |
| 상태 변경 | DRAFT → CONFIRMED → SENT → COMPLETED |
| PDF 출력 | A4 사이즈 발주서 PDF 생성 |
| Excel 출력 | 발주서 Excel 다운로드 |
| 이력 조회 | 해당 발주서의 상태 변경 이력 |

### 6.5 베트남 주간 발주

베트남 발주는 주간 단위로 통합하여 처리합니다.

| 기능 | 설명 |
|------|------|
| 주간 누적 조회 | 해당 주에 발주 필요한 품목 목록 |
| 선적 예정일 선택 | Date Picker로 날짜 선택 |
| 통합 발주서 생성 | 선택한 품목들을 하나의 발주서로 |
| 예상 입고일 표시 | 선적일 + 리드타임(21일) |

---

## 7. MRP 계산 로직 상세

### 7.1 소요량 산출

| 상황 | 계산 방식 |
|------|----------|
| 출고 계획 있음 | 해당 계획 수량 사용 |
| 출고 계획 없음 또는 불충분 | **평균 납품량** 사용 |

```
소요량 = MAX(출고 계획 수량, 평균 납품량)
```

### 7.2 평균 납품량 계산

```
평균 납품량 = Σ(최근 N일 출고 수량) / N
```

| 설정 | 값 |
|------|-----|
| **기본 기간 (N)** | 365일 (1년) |
| **사용자 설정** | 시스템 설정에서 변경 가능 |

> 📌 시스템 설정 화면에서 평균 납품량 기준 기간을 변경할 수 있습니다.

### 7.3 발주량 계산

```
발주량 = 소요량 - 현재고 - 입고예정 + 안전재고
```

**발주량 > 0 인 경우에만 발주 권고 표시**

### 7.4 발주일 계산

```
발주일 = 납품일 - 리드타임
```

| 공급처 | 리드타임 | 안전재고 계수 |
|--------|---------|--------------|
| 국내 협력사 | 3~7일 (품목별 설정) | 1.2 |
| 베트남 공장 | 14~21일 (품목별 설정) | 1.5 |

### 7.5 이원화 공급 (BOTH)

주공급처가 'BOTH'인 경우:

```
국내 발주량 = Round(총 발주량 × 국내 비율 / 100)
베트남 발주량 = 총 발주량 - 국내 발주량
```

---

## 8. 데이터베이스 추가 설계

### 8.1 신규 테이블

#### initial_inventory (초기 재고)
```sql
CREATE TABLE initial_inventory (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    product_id UUID REFERENCES products(id),
    base_date DATE NOT NULL,                    -- 기준일
    quantity INTEGER NOT NULL,                  -- 초기 수량
    remarks TEXT,
    created_by UUID,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 품목당 기준일별 1건만 허용
CREATE UNIQUE INDEX idx_initial_inventory_unique 
ON initial_inventory(product_id, base_date);
```

#### inventory_adjustments (재고 조정)
```sql
CREATE TYPE adjustment_type AS ENUM ('INCREASE', 'DECREASE');
CREATE TYPE adjustment_reason AS ENUM ('PHYSICAL_COUNT', 'LOSS', 'DAMAGE', 'OTHER');

CREATE TABLE inventory_adjustments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    adjustment_date DATE NOT NULL,              -- 조정일
    product_id UUID REFERENCES products(id),
    adjustment_type adjustment_type NOT NULL,   -- 증가/감소
    quantity INTEGER NOT NULL,                  -- 조정 수량 (양수)
    reason adjustment_reason NOT NULL,          -- 조정 사유
    remarks TEXT,
    created_by UUID,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### system_settings (시스템 설정)
```sql
CREATE TABLE system_settings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    setting_key VARCHAR(100) UNIQUE NOT NULL,
    setting_value TEXT NOT NULL,
    description TEXT,
    updated_by UUID,
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 기본 설정값 삽입
INSERT INTO system_settings (setting_key, setting_value, description) VALUES
('avg_shipment_days', '365', '평균 납품량 계산 기준 일수'),
('domestic_safety_factor', '1.2', '국내 안전재고 계수'),
('vietnam_safety_factor', '1.5', '베트남 안전재고 계수');
```

### 8.2 뷰 (View)

#### v_current_inventory (현재 재고 뷰)
```sql
CREATE VIEW v_current_inventory AS
SELECT 
    p.id AS product_id,
    p.product_code,
    p.product_name,
    vm.vehicle_name,
    COALESCE(init.quantity, 0) AS initial_qty,
    COALESCE(SUM(CASE WHEN it.transaction_type = 'IN' THEN it.quantity ELSE 0 END), 0) AS in_qty,
    COALESCE(SUM(CASE WHEN it.transaction_type = 'OUT' THEN it.quantity ELSE 0 END), 0) AS out_qty,
    COALESCE(SUM(CASE WHEN ia.adjustment_type = 'INCREASE' THEN ia.quantity 
                      WHEN ia.adjustment_type = 'DECREASE' THEN -ia.quantity 
                      ELSE 0 END), 0) AS adj_qty,
    (COALESCE(init.quantity, 0) 
     + COALESCE(SUM(CASE WHEN it.transaction_type = 'IN' THEN it.quantity ELSE 0 END), 0)
     - COALESCE(SUM(CASE WHEN it.transaction_type = 'OUT' THEN it.quantity ELSE 0 END), 0)
     + COALESCE(SUM(CASE WHEN ia.adjustment_type = 'INCREASE' THEN ia.quantity 
                        WHEN ia.adjustment_type = 'DECREASE' THEN -ia.quantity 
                        ELSE 0 END), 0)
    ) AS current_qty,
    p.safety_stock_min AS safety_stock
FROM products p
LEFT JOIN vehicle_models vm ON p.vehicle_model_id = vm.id
LEFT JOIN initial_inventory init ON p.id = init.product_id
LEFT JOIN inventory_transactions it ON p.id = it.product_id
LEFT JOIN inventory_adjustments ia ON p.id = ia.product_id
WHERE p.is_active = true
GROUP BY p.id, p.product_code, p.product_name, vm.vehicle_name, init.quantity, p.safety_stock_min;
```

---

## 9. 개발 가이드라인

### 9.1 기술 스택 (유지)

| 구분 | 기술 |
|------|------|
| Frontend | React + TypeScript |
| UI Framework | shadcn/ui + TanStack Table |
| Backend | Supabase |
| Architecture | **Clean Architecture** |

### 9.2 MCP 활용

| MCP 서버 | 활용 |
|----------|------|
| **figma** | UI 와이어프레임, 디자인 시안 |
| **shadcn** | UI 컴포넌트 생성 |
| **magic** | 대시보드, 차트, 고급 UI |
| **supabase** | DB, Auth, Realtime |
| **postgres** | SQL 쿼리, 뷰 생성 |
| **sequential-thinking** | MRP 알고리즘 설계 |
| **playwright** | E2E 테스트 |
| **context7** | 라이브러리 문서 참조 |

### 9.3 개발 방식

> 📌 **feature-planner 스킬**을 사용하여 각 기능을 단계별로 구현합니다.

```
개발 순서 (Claude Code):
1. feature-planner로 기능 분석 및 계획 수립
2. figma MCP로 UI 디자인
3. Clean Architecture 구조에 맞게 코드 작성
4. playwright로 테스트
```

### 9.4 Clean Architecture 구조

```
src/
├── domain/
│   ├── entities/
│   │   ├── Inventory.ts          # 재고 엔티티
│   │   ├── InitialInventory.ts   # 초기재고 엔티티
│   │   └── InventoryAdjustment.ts # 재고조정 엔티티
│   └── repositories/
│       └── IInventoryRepository.ts
│
├── application/
│   ├── usecases/
│   │   ├── CalculateCurrentStock.ts    # 현재고 계산
│   │   ├── RegisterInitialInventory.ts # 초기재고 등록
│   │   ├── AdjustInventory.ts          # 재고 조정
│   │   └── CalculateMRP.ts             # MRP 계산 (수정)
│   └── services/
│       └── InventoryService.ts
│
├── interface/
│   └── controllers/
│       └── InventoryController.ts
│
└── infrastructure/
    └── supabase/
        └── InventoryRepository.ts
```

---

## 10. 개발 일정

### 10.1 Phase 1 수정 (MVP) - 10주

| 주차 | 영역 | 작업 내용 | MCP 활용 |
|------|------|----------|----------|
| 1주 | 설계 | UI 와이어프레임 (전체 화면) | figma |
| 2주 | 프로젝트 셋업 | DB 스키마 (추가 테이블 포함), Auth | supabase, postgres |
| 3주 | 기준관리 | 회사정보, 차종, 거래처, 제품, 환율 | shadcn, context7 |
| 4주 | 입고/출고 | 입고 현황, 출고 현황 (새 레이아웃) | shadcn, magic |
| 5주 | **재고 관리** | 재고 현황, 초기재고, 재고조정 | shadcn, postgres |
| 6주 | 발주 관리 (1) | 출고 계획 등록 | shadcn |
| 7주 | 발주 관리 (2) | MRP 계산 / 발주 권고 | sequential-thinking |
| 8주 | 발주 관리 (3) | 발주서 관리, 베트남 주간 발주 | shadcn, magic |
| 9주 | 대시보드 | KPI, 차트, 알림 | magic |
| 10주 | 테스트/마무리 | E2E 테스트, 버그 수정 | playwright |

### 10.2 Phase 2 (고도화) - 추후

- 보고서 기능
- Slack/Notion 연동
- 직인 이미지 자동 날인
- 고급 분석

---

## 부록: 화면 목록

| 메뉴 | 화면명 | 우선순위 |
|------|--------|---------|
| 대시보드 | 대시보드 | P1 |
| 기준관리 | 회사정보 | P1 |
| 기준관리 | 차종관리 | P1 |
| 기준관리 | 거래처관리 | P1 |
| 기준관리 | 제품관리 | P1 |
| 기준관리 | 환율관리 | P1 |
| 입고/출고 | 입고 현황 | P1 |
| 입고/출고 | 출고 현황 | P1 |
| 재고 관리 | 재고 현황 | P1 |
| 재고 관리 | 초기 재고 등록 | P1 |
| 재고 관리 | 재고 조정 | P1 |
| 발주 관리 | 출고 계획 등록 | P1 |
| 발주 관리 | MRP 계산 / 발주 권고 | P1 |
| 발주 관리 | 발주서 관리 | P1 |
| 발주 관리 | 베트남 주간 발주 | P1 |
| 보고서 | (추후 정의) | P2 |

---

**— 문서 끝 —**
